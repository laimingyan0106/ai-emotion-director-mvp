[project]
name = "ai-emotion-director-api"
version = "1.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.116.1",
  "python-multipart==0.0.20",
  "psycopg[binary,pool]==3.2.9",
  "pydantic-settings==2.10.1",
  "httpx==0.28.1",
]

[tool.vercel]
entrypoint = "app.main:app"
